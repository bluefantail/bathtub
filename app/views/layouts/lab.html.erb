<!DOCTYPE html>
<html>
  <head>
    <!-- Set the size for mobile devices -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bathtub Film</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

    <!-- Fonts -->
    <link href='https://fonts.googleapis.com/css?family=Roboto:400,700,900,900italic,400italic,700italic' rel='stylesheet' type='text/css'>
  </head>

  <body>
    <div id="foundation">
      <div id="house">
        <h1>Site Title</h1>
        <h3>Lab</h3>
        <nav>
          <ul>
            <li><%= link_to "Bath Manager", lab_path, :class=> active_class(lab_path) %></li>
            <li><%= link_to "Tasks", lab_tasks_path, :class=> active_class(lab_tasks_path) %></li>
            <li><%= link_to "Settings", lab_settings_path, :class=> active_class(lab_settings_path) %></li>
          </ul>
        </nav>
        <div id="bathroom">
          <%= yield %>
        </div>
        <%= link_to "Back to main site", root_path, :class=> "link" %>
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>
    </div>
    <%= link_to("Logout", destroy_user_session_path, :method => :delete) %>
  </body>
</html>